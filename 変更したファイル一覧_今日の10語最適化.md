# 変更したファイル

- `glossaryApp.js`：今日の10語専用の候補選定、日別回数、直前回、最終出題日、開始済みセッション復元、2回目正解直後の3日間隔を追加。
- `style.css`：トップページの装飾用 `🔍` を `💈` へ変更。
- `sw.js`、`教材/sw.js`、`過去問/sw.js`：公開版3.0.0を維持したまま内部キャッシュ識別子を `r1` へ更新。

用語・3択・問題集・教材・実過去問のデータファイルは変更していません。
