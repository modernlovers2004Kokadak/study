# バックアップ・復元・リセット互換性

追加情報は既存の用語集保存オブジェクト内に含まれるため、既存のバックアップ対象キーを増減していません。既存バックアップは追加項目なしとして読み込めます。

用語集専用リセットおよび全体初期化は、従来どおり `riyoshi_glossary_learning_v1` を削除するため、`__today10` も同時に削除されます。他機能の履歴キーには触れません。
